import Link from "next/link";
import { ArrowLeft, ShieldCheck, Sparkles } from "lucide-react";
import { BrandMark } from "@/components/shared/shared";

export function AuthShell({eyebrow,title,description,children}:{eyebrow:string;title:string;description:string;children:React.ReactNode}){return <main className="grid min-h-dvh bg-background lg:grid-cols-[minmax(0,1fr)_minmax(520px,.85fr)]"><section className="relative hidden overflow-hidden bg-sidebar p-10 text-white lg:flex lg:flex-col"><div className="absolute inset-0 opacity-50 [background:radial-gradient(circle_at_20%_20%,#f05a3d45,transparent_32%),radial-gradient(circle_at_80%_75%,#8b5cf630,transparent_30%)]"/><div className="relative"><BrandMark dark/></div><div className="relative my-auto max-w-lg"><span className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/5 px-3 py-1.5 text-xs font-semibold text-white/75"><Sparkles className="h-3.5 w-3.5"/>Progress feels better together</span><h2 className="mt-6 text-5xl font-bold leading-[1.05] tracking-[-.05em]">Make showing up your strongest habit.</h2><p className="mt-5 text-lg text-white/60">Track the day, protect the streak, and let a supportive community turn consistency into momentum.</p><div className="mt-10 grid grid-cols-3 gap-3">{[["12 days","Current streak"],["86%","Completion"],["Level 8","3,450 XP"]].map(([value,label])=><div key={label} className="rounded-xl border border-white/10 bg-white/5 p-4"><strong className="block text-xl">{value}</strong><span className="text-xs text-white/50">{label}</span></div>)}</div></div><div className="relative flex items-center gap-2 text-xs text-white/45"><ShieldCheck className="h-4 w-4"/>Your demo progress stays on this device.</div></section><section className="flex min-h-dvh items-center justify-center p-5 sm:p-8"><div className="w-full max-w-md"><div className="mb-8 lg:hidden"><BrandMark/></div><Link href="/" className="mb-8 inline-flex items-center gap-2 text-sm text-muted hover:text-foreground"><ArrowLeft className="h-4 w-4"/>Back to StreakUp</Link><div className="mb-7"><div className="text-xs font-bold uppercase tracking-[.15em] text-primary">{eyebrow}</div><h1 className="mt-2 text-3xl font-bold tracking-[-.04em]">{title}</h1><p className="mt-2 text-sm text-muted">{description}</p></div>{children}</div></section></main>}
export function SocialButtons(){return <div className="grid grid-cols-2 gap-3"><button className="flex h-11 items-center justify-center gap-2 rounded-[10px] border bg-surface text-sm font-semibold hover:bg-surface-2"><span className="text-base font-bold">G</span>Google</button><button className="flex h-11 items-center justify-center gap-2 rounded-[10px] border bg-surface text-sm font-semibold hover:bg-surface-2"><span className="grid h-5 w-5 place-items-center rounded-full bg-foreground text-[10px] text-background">GH</span>GitHub</button></div>}
export function AuthDivider(){return <div className="my-5 flex items-center gap-3 text-xs text-muted"><span className="h-px flex-1 bg-border"/>or continue with email<span className="h-px flex-1 bg-border"/></div>}
