"use client";

import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {Button} from "@/components/ui/button";import {Input,Label} from "@/components/ui/primitives";import {AuthDivider,AuthShell,SocialButtons} from "@/components/auth/auth-shell";
export default function SignupPage(){const router=useRouter();const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const valid=password.length>=8&&password===confirm;return <AuthShell eyebrow="Start your streak" title="Build a system you’ll want to keep." description="Your first habit is only a few minutes away."><SocialButtons/><AuthDivider/><form className="space-y-4" onSubmit={e=>{e.preventDefault();router.push("/onboarding")}}><div className="grid grid-cols-2 gap-3"><div><Label htmlFor="name">Name</Label><Input id="name" placeholder="Alex Morgan" required/></div><div><Label htmlFor="username">Username</Label><Input id="username" placeholder="alexmorgan" required minLength={3}/></div></div><div><Label htmlFor="signup-email">Email</Label><Input id="signup-email" type="email" placeholder="alex@example.com" required/></div><div><Label htmlFor="signup-password">Password</Label><Input id="signup-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/><p className="mt-1 text-xs text-muted">At least 8 characters.</p></div><div><Label htmlFor="confirm-password">Confirm password</Label><Input id="confirm-password" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/>{confirm&&confirm!==password&&<p className="mt-1 text-xs text-danger">Passwords do not match.</p>}</div><Button className="w-full" size="lg" disabled={!valid}>Create Account</Button></form><p className="mt-6 text-center text-sm text-muted">Already a member? <Link href="/login" className="font-semibold text-primary">Sign in</Link></p></AuthShell>}
