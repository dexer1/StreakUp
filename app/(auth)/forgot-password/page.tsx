"use client";

import {useState} from "react";import Link from "next/link";import {CheckCircle2} from "lucide-react";import {Button} from "@/components/ui/button";import {Input,Label} from "@/components/ui/primitives";import {AuthShell} from "@/components/auth/auth-shell";
export default function ForgotPage(){const [sent,setSent]=useState(false);return <AuthShell eyebrow="Account recovery" title="Reset your password." description="We’ll send a secure reset link to your inbox.">{sent?<div className="rounded-xl border border-success/20 bg-success-soft p-5 text-center"><CheckCircle2 className="mx-auto h-8 w-8 text-success"/><h2 className="mt-3 font-bold">Check your inbox</h2><p className="mt-1 text-sm text-muted">If an account exists for that address, your link is on its way.</p><Button asChild className="mt-5" variant="secondary"><Link href="/login">Back to Sign In</Link></Button></div>:<form onSubmit={e=>{e.preventDefault();setSent(true)}}><Label htmlFor="reset-email">Email</Label><Input id="reset-email" type="email" placeholder="alex@example.com" required/><Button className="mt-4 w-full" size="lg">Send Reset Link</Button></form>}</AuthShell>}
