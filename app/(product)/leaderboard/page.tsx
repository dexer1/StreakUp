"use client";

import { useMemo, useState } from "react";
import { Flame } from "lucide-react";
import { Card } from "@/components/ui/primitives";
import { PageHeader, UserAvatar } from "@/components/shared/shared";
import { currentUser, leaderboard, users } from "@/data/mock-data";
import { cn } from "@/lib/utils";

type Category="XP"|"Current Streak"|"Longest Streak"|"Challenges Completed";
export default function LeaderboardPage(){const [scope,setScope]=useState("Friends");const [time,setTime]=useState("This Week");const [category,setCategory]=useState<Category>("XP");const rows=useMemo(()=>[...leaderboard].sort((a,b)=>{const key=category==="XP"?"xp":category==="Current Streak"?"currentStreak":category==="Longest Streak"?"longestStreak":"challengesCompleted";return b[key]-a[key]}).map((e,i)=>({...e,rank:i+1})),[category]);return <div className="page-wrap"><PageHeader title="Leaderboard" description="Friendly competition, measured by the progress that matters."/><div className="mb-5 flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div className="flex rounded-xl border bg-surface p-1">{["Friends","Global"].map(v=><button key={v} onClick={()=>setScope(v)} className={cn("rounded-lg px-4 py-2 text-sm font-semibold",scope===v?"bg-foreground text-background":"text-muted")}>{v}</button>)}</div><div className="scrollbar-none flex gap-2 overflow-x-auto"><select className="rounded-lg border bg-surface px-3 py-2 text-sm" value={time} onChange={e=>setTime(e.target.value)}>{["This Week","This Month","All Time"].map(v=><option key={v}>{v}</option>)}</select>{(["XP","Current Streak","Longest Streak","Challenges Completed"] as Category[]).map(v=><button key={v} onClick={()=>setCategory(v)} className={cn("whitespace-nowrap rounded-lg border px-3 py-2 text-xs font-semibold",category===v?"border-primary bg-primary-soft text-primary":"bg-surface text-muted")}>{v}</button>)}</div></div><div className="mb-5 grid gap-3 md:grid-cols-3">{rows.slice(0,3).map((entry,i)=>{const user=users.find(u=>u.id===entry.userId)!;return <Card key={entry.userId} className={cn("relative overflow-hidden p-5 text-center",i===0&&"border-primary/30 md:-translate-y-1")}><div className={cn("absolute left-3 top-3 grid h-8 w-8 place-items-center rounded-full text-sm font-bold",i===0?"bg-primary text-white":"bg-surface-2")}>{entry.rank}</div><UserAvatar user={user} size="lg" className="mx-auto"/><h2 className="mt-3 font-bold">{user.name}</h2><p className="text-xs text-muted">Level {user.level}</p><div className="mt-4 text-xl font-bold text-primary">{category==="XP"?`${entry.xp.toLocaleString()} XP`:category.includes("Streak")?`${category==="Current Streak"?entry.currentStreak:entry.longestStreak} days`:`${entry.challengesCompleted} wins`}</div></Card>})}</div><Card className="overflow-hidden"><div className="hidden grid-cols-[60px_1fr_100px_120px_130px] items-center gap-3 border-b bg-surface-2 px-5 py-3 text-xs font-bold uppercase tracking-wider text-muted md:grid"><span>Rank</span><span>Member</span><span>Level</span><span>Streak</span><span className="text-right">XP</span></div><div className="divide-y divide-border">{rows.map(entry=>{const user=users.find(u=>u.id===entry.userId)!;const you=user.id===currentUser.id;return <div key={entry.userId} className={cn("grid gap-3 p-4 md:grid-cols-[60px_1fr_100px_120px_130px] md:items-center md:px-5",you&&"bg-primary-soft")}><div className="hidden font-bold md:block">#{entry.rank}</div><div className="flex items-center gap-3"><span className="font-bold md:hidden">#{entry.rank}</span><UserAvatar user={user} size="sm"/><div><div className="font-semibold">{user.name}{you&&" (You)"}</div><div className="text-xs text-muted">@{user.username}</div></div></div><div className="flex items-center justify-between text-sm md:block"><span className="text-muted md:hidden">Level</span><span>Level {user.level}</span></div><div className="flex items-center justify-between text-sm md:block"><span className="text-muted md:hidden">Current streak</span><span className="inline-flex items-center gap-1 font-semibold"><Flame className="h-4 w-4 text-primary"/>{entry.currentStreak} days</span></div><div className="flex items-center justify-between font-bold md:text-right"><span className="text-sm font-normal text-muted md:hidden">XP</span>{entry.xp.toLocaleString()} XP</div></div>})}</div></Card></div>}
